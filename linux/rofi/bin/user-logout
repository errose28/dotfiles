#!/usr/bin/env sh

wm="$(wmctrl -m | grep Name: | awk '{print $2}')"

if [ "$wm" = i3 ]; then
    i3-msg exit
elif [ "$wm" = bspwm ]; then
   bspc quit 
else
    echo "Unsure how to log out of window manager $wm" 1>&2
fi
