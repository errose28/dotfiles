#!/usr/bin/env sh

# TODO: Make this work on the current display only.

pos_file=/tmp/menu_mouse_pos

if [ -f "$pos_file" ]; then
    cliclick m:"$(cat "$pos_file")"
    rm "$pos_file"
else
    mouse_pos="$(cliclick p)"
    cliclick 'm:500,0'
    echo "$mouse_pos" > "$pos_file"
fi




